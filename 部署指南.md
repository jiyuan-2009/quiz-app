# 🚀 Render 部署手把手操作指南

> 预计用时：5 ~ 10 分钟
> 费用：**完全免费**，无需信用卡
> 最终你会得到一个 `https://xxx.onrender.com` 的公网访问地址

---

## 第 0 步：准备工作（提前准备好）

部署前先把下面这些准备好，部署时一路复制粘贴就行：

| 要准备的东西 | 从哪来 | 备注 |
|-------------|--------|------|
| GitHub 账号 | [github.com](https://github.com) 注册 | 免费，用邮箱注册就行 |
| 飞书 App ID | 飞书开放平台 → 你的应用 → 凭证与基础信息 | `cli_` 开头 |
| 飞书 App Secret | 同上 | 一长串字符 |
| 多维表格 Base Token | 已给你：`W20CbpxemaMpuesZTSwcwE7enCf` | 直接用 |

---

## 第 1 步：把代码上传到 GitHub

### 1.1 新建 GitHub 仓库

1. 登录 GitHub，点击右上角 **+** → **New repository**
2. Repository name 填：`quiz-app`
3. 选 **Public**（公开，免费版也能用）
4. 勾选 **Add a README file**（不勾也行）
5. 点击 **Create repository**

### 1.2 上传代码文件

1. 进入刚创建的仓库页面
2. 点击 **Add file** → **Upload files**
3. 解压 `二维码答题系统(多人共享版).zip`
4. 把 `quiz-app` 文件夹里的**所有文件**（注意是里面的文件，不是文件夹本身）拖拽到网页上传区域
   - `server.js`
   - `package.json`
   - `render.yaml`
   - `.env.example`
   - `README.md`
   - `public/` 文件夹（整个拖进去）
5. 往下拉，点击 **Commit changes**

> ✅ 验证：仓库首页能看到 `server.js`、`package.json` 等文件，就说明上传成功了。

---

## 第 2 步：注册 Render 账号

1. 打开 [render.com](https://render.com)
2. 点击右上角 **Get Started**
3. 选 **Sign up with GitHub**（用 GitHub 账号登录，最方便）
4. 授权 Render 访问你的 GitHub 账号（点 Authorize render-labs）

> 💡 Render 免费版不需要绑定信用卡，放心注册。

---

## 第 3 步：创建 Web 服务

1. 登录 Render 后，点击顶部的 **New +**
2. 选择 **Web Service**
3. 在列表里找到你刚创建的 `quiz-app` 仓库，点击右边的 **Connect**
   - 如果看不到，点 **Configure account** → 给 Render 授权这个仓库

### 填写配置（重点！）

按下面的表格逐项填写：

| 配置项 | 填什么 | 说明 |
|--------|--------|------|
| **Name** | `quiz-app`（或你喜欢的名字） | 这个名字会变成访问地址的一部分，比如 `quiz-app.onrender.com` |
| **Region** | 选 **Singapore** (新加坡) | 离国内近，访问速度快 |
| **Branch** | `main` | 默认就是 main，不用改 |
| **Runtime** | **Node** | 默认就是 Node，不用改 |
| **Build Command** | `npm install` | 复制粘贴 |
| **Start Command** | `npm start` | 复制粘贴 |
| **Instance Type** | 选 **Free** | 免费版 |

### 添加环境变量（最重要的一步）

往下滚动，找到 **Environment Variables** 区域，点击 **Add Environment Variable**，逐个添加以下 7 个变量：

| Key | Value | 说明 |
|-----|-------|------|
| `FEISHU_APP_ID` | `cli_xxxxxxxxxxxx` | 替换成你的飞书 App ID |
| `FEISHU_APP_SECRET` | 一长串字符 | 替换成你的飞书 App Secret |
| `FEISHU_BASE_TOKEN` | `W20CbpxemaMpuesZTSwcwE7enCf` | 直接复制 |
| `QUIZ_TABLE_ID` | `tblQF6QmmICc4mEp` | 直接复制 |
| `RECORD_TABLE_ID` | `tblhvkWe0fmSVEz2` | 直接复制 |
| `ADMIN_PASSWORD` | `你自己设一个密码` | 题库管理的登录密码，别用默认的 |
| `PORT` | `10000` | Render 默认端口，必须填 10000 |

> ⚠️ **注意**：飞书 App ID 和 App Secret 一定要填对，不然系统无法读取题库。

### 开始部署

所有配置填好后，点击页面最下方的 **Create Web Service** 按钮。

---

## 第 4 步：等待部署完成

点击 Create 后，会进入部署日志页面，你会看到一大堆日志在滚动。

- 正常情况下，**2 ~ 3 分钟** 就能部署完成
- 成功标志：日志最后出现类似这样的文字：
  ```
  ==> Build successful 
  ==> Deploying...
  ==> Your service is live 🎉
  ```
- 页面顶部会显示你的访问地址，格式是：`https://你的应用名.onrender.com`

> 💡 如果部署失败了，别慌！看日志里的红色错误信息，通常是环境变量填错了。截图发给我，我帮你看。

---

## 第 5 步：验证部署成功

1. 打开你的访问地址（`https://xxx.onrender.com`）
2. 能看到答题系统首页 → ✅ 前端正常
3. 点击「开始答题」→ 填信息 → 点「开始答题」
   - 如果能加载出题目 → ✅ 后端 API 和飞书连接正常
   - 如果报错「加载题目失败」→ 检查飞书 App ID / Secret / 权限配置

---

## 第 6 步：生成答题二维码

1. 访问你的系统首页
2. 点击「二维码」
3. 用手机扫一下二维码
4. 手机能打开答题页面 → ✅ 部署完全成功

把这个二维码打印出来或发到群里，大家就能扫码答题了！

---

## ⚠️ 免费版注意事项

1. **冷启动**：15 分钟没人访问，服务会自动休眠。下次第一个人打开时，需要等 20~30 秒才能加载出来（之后就正常了）
   - 💡 活动开始前，你自己先打开一次把它「唤醒」
2. **免费额度**：每月 750 小时免费时长 = 一个服务全天候运行，完全够用
3. **带宽**：每月 100GB 免费流量，答题活动绰绰有余

---

## 🔧 常见问题

### Q: 部署失败，日志里有红色错误？
A: 最常见的原因：
- 环境变量名写错了（注意大小写，必须完全一致）
- 飞书 App ID 或 Secret 填错了
- 代码文件没传全（检查 `server.js` 和 `package.json` 在不在仓库根目录）

### Q: 页面能打开，但题目加载不出来？
A: 检查以下几点：
1. 飞书应用是否开通了 `bitable:app`、`bitable:record:read` 权限
2. 飞书应用是否已发布（不是草稿状态）
3. 多维表格是否已把应用添加为协作者（可编辑权限）
4. 环境变量里的 Base Token 和 Table ID 是否正确

### Q: 想改管理员密码？
A: Render 后台 → 你的服务 → Environment → 找到 `ADMIN_PASSWORD` → 修改 → 保存后会自动重新部署。

### Q: 想换个更好记的地址？
A: Render 免费版支持绑定自定义域名。Settings → Custom Domains → Add Custom Domain。

### Q: 想更新题库，除了在系统里改，还有别的方式吗？
A: 有！直接在[飞书多维表格](https://envision-energy.feishu.cn/base/W20CbpxemaMpuesZTSwcwE7enCf)里编辑「题库」表就行，改完立即生效，不需要重新部署。

---

## 📞 遇到问题怎么办？

把以下信息发给我，我帮你排查：
1. 部署日志里的错误截图
2. 你的 Render 服务地址
3. 环境变量的 Key 列表（不用发 Value，只发 Key 名就行）

---

**祝部署顺利！有任何问题随时找我～** 🎉
